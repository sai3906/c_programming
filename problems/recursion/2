#include<stdio.h>
int arr_rev(int a[],int );
int main(){
  int a[]={1,2,3,4,5,6};
  int len = sizeof(a)/sizeof(a[1]);
  arr_rev(a,len);
}
int arr_rev(int *b,int len){
  printf("len of b=%d\n",len);
  for(int i=0;i<len;i++){
    printf("%d ",b[i]);
  }
  printf("\n");
}
